# 音乐 6.8.0 逆向整理与复刻规划

## 1. 当前结论

- `reverse/apk/音乐_6.8.0.apk` 已完成首轮反编译，输出目录为 `reverse/decompiled/音乐_6.8.0/`。
- 这个包当前呈现为 **系统预装资源包形态**：APK 归档内可见 `AndroidManifest.xml`、`resources.arsc`、`res/`、`assets/`、`META-INF/` 等资源目录，归档检查结果为 `classes.dex=0`、`.so=0`、`.jar=0`。
- 这类结构很符合旧版 ROM 内置应用的交付方式，Java 代码通常位于同版本系统镜像里的 `oat/odex/vdex`。当前 APK 已经足够支撑 **静态资源整理、像素级 UI 复刻、交互结构规划**。
- 当前工程 `app` 已完成基础壳层、Media3 本地播放链路、歌曲列表、歌曲编辑态隐藏管理、主播放页复刻、更多动作面板首轮复刻、Scratch 搓碟链路、原版唱针资源还原、唱片视觉时钟连续旋转优化、专辑列表 / 平铺与专辑详情二级页复刻、艺术家列表与歌手详情页首轮复刻、播放列表模块落地与二轮细节校正、更多页首屏入口复刻、设置二级页与播放设置持久化联动、文件夹页目录聚合与资料库隐藏管理首轮接入、删除 / 隐藏后的全局数据回收、媒体库手动重扫链路、风格页本地流派聚合与详情二级页、我喜欢的歌曲页与收藏持久化链路、壳层标题栏与更多页二级页返回按钮的统一样式校正、壳层标题栏按钮质感重做、壳层级全局搜索页与本地搜索历史 / 歌曲 / 专辑 / 艺术家分类结果链路、搜索页输入框清除按钮尺寸与按下态校正、播放页“设置铃声 / 定时关闭播放 / 删除本地歌曲”真实业务、播放队列页 Compose 浮层与继续播放清空 / 拖拽排序链路，以及歌曲页“歌手 - 专辑”副标题、无损格式徽标和应用名“音乐”对齐，包名也已经和原版对齐为 `com.smartisanos.music`，适合继续沿真实音频路由和 1:1 细节校正方向推进。

## 2. APK 基础信息

- 包名：`com.smartisanos.music`
- `versionCode`：`4`
- `versionName`：`8.1.0`
- 原版 `minSdkVersion`：`19`
- 原版 `targetSdkVersion`：`23`
- Application：`.SmartisanApplication`
- 启动页：`.activities.MusicMain`
- 主播放页：`.activities.PlaybackActivity`
- 主播放服务：`.service.SmartisanMusicService`

和复刻最相关的组件入口：

- 首页容器：`res/layout/a_music_main.xml`
- 播放页容器：`res/layout/a_playback.xml`
- 主播放界面：`res/layout/audio_player.xml`
- 主控制面板：`res/layout/ll_controller.xml`
- 更多动作面板：`res/layout/rl_more_action.xml`
- 小播放条：`res/layout/playback_bar.xml`
- 队列页：`res/layout/now_playing_layout.xml`

## 3. 静态资源盘点

`reverse/decompiled/音乐_6.8.0/resources/res/` 关键目录数量：

- `layout`：`254`
- `drawable`：`251`
- `drawable-xxhdpi-v4`：`781`
- `drawable-400dpi-v4`：`39`
- `drawable-560dpi-v4`：`4`
- `anim`：`16`
- `raw`：`42`
- `menu`：`4`
- `xml`：`2`

高价值资源集中在 `drawable-xxhdpi-v4`、`layout`、`values-xxhdpi-v4/dimens.xml`。

### 3.1 播放页核心位图

唱盘与唱针层：

- `playing_lp.png`
- `playing_cover_lp.png`
- `playing_cover_lp_slow.png`
- `playing_stylus_lp_bg.9.png`
- `playing_stylus_lp.9.png`
- `playing_stylus_lp_top.9.png`
- `needle_shadow2.png`
- `mask_playing_lyric.png`

控制与状态层：

- `playing_control_time.png`
- `playing_control_volume.png`
- `progressbar_time_bg.9.png`
- `progressbar_time.9.png`
- `playing_progress_bar_line.png`
- `btn_playing_prev.png`
- `btn_playing_play.png`
- `btn_playing_pause.png`
- `btn_playing_next.png`
- `btn_playing_shuffle_off.9.png`
- `btn_playing_shuffle_on.9.png`
- `btn_playing_cycle_off.9.png`
- `btn_playing_cycle_on.9.png`
- `btn_playing_audio_quality_off.png`
- `btn_playing_audio_quality_on.png`
- `btn_playing_hifi_off.png`
- `btn_playing_hifi_on.png`
- `btn_playing_scratche_off.png`
- `btn_playing_scratche_on.png`

顶部与辅助动作：

- `more_btn.png`
- `more_btn_down.png`
- `sun_btn_off.png`
- `sun_btn_off_down.png`
- `sun_btn_on.png`
- `sun_btn_on_down.png`
- `playing_dot_black.png`
- `playing_dot_white.png`
- `playing_dot_bluetooth_black.png`
- `playing_dot_bluetooth_white.png`

小播放条：

- `floatplay_bg.9.png`
- `now_playing_bar_shadow.png`
- `floatplay_btn_prev.png`
- `floatplay_btn_play.png`
- `floatplay_btn_pause.png`
- `floatplay_btn_next.png`
- `floatplay_btn_favorite_add.png`
- `floatplay_btn_favorite_cancel.png`

### 3.2 播放页相关 XML 资源

- `drawable/seekbar_progress.xml`
- `drawable/volume_seekbar_progress.xml`
- `drawable/light_off_selector.xml`
- `drawable/light_on_selector.xml`
- `drawable/more_btn_selector.xml`
- `drawable/rb_playing_dot.xml`
- `drawable/rb_playing_dot_bluetooth.xml`
- `drawable/rb_btn_playing_audio_quality.xml`
- `drawable/rb_btn_playing_hifi.xml`
- `drawable/rb_btn_playing_scratch.xml`

### 3.3 音效资源

`res/raw` 内已有 42 个音频资源，适合后续做交互补完：

- `collect.ogg`
- `cancel.ogg`
- `a_1.ogg` 到 `a_20.ogg`
- `y_1_01.ogg` 到 `y_20_01.ogg`

这批资源大概率覆盖收藏、取消收藏、模拟黑胶底噪或机械反馈音。

## 4. 播放页结构拆解

### 4.1 主体布局树

`a_playback.xml` 的结构非常清晰：

1. 底层是专辑或曲目容器 `fl_album_track_container`
2. 中层 `include @layout/audio_player`
3. 顶层是 Smartisan 自定义标题栏

`audio_player.xml` 可以直接映射为 Compose 组件树：

1. 顶部进度区
   - `PlaybackSeekBar`
   - 一条极细的 `playing_progress_bar_line`
2. 中央转盘区 `top_slid`
   - `radio_cover`
   - `turntable_view`
   - `Turntables audio_player_album_art`
   - `needle_shadow`
   - `NeedleView needle`
   - `playing_stylus_lp_top`
   - `LrcView audio_lrc`
   - `screen_switch_btn`
   - `btn_action_more`
3. 下部翻页区
   - `vp_song_handler`
   - `rb_bluetooth_dot`
   - `rb_controller_dot`

### 4.2 拟物层级

从视觉和资源组织看，播放页至少有 6 层：

1. 唱盘底座层：`playing_lp`
2. 唱片主体层：`playing_cover_lp`
3. 唱片标签与纹理层：已经合成在 `playing_cover_lp`
4. 唱针基座层：`playing_stylus_lp_bg` + `playing_stylus_lp_top`
5. 唱针本体层：`playing_stylus_lp`
6. 阴影与遮罩层：`needle_shadow2` + `mask_playing_lyric`

这个层级非常适合拆成独立 Compose 组件，后续可以分别控制：

- 唱片旋转
- 唱针摆角
- 阴影跟随
- 歌词遮罩显隐
- 灯光按钮状态

### 4.3 控制区结构

`ll_controller.xml` 是主控制页，包含：

- 重复模式按钮
- 上一首
- 播放/暂停
- 下一首
- 随机模式
- 音量 SeekBar

`rl_more_action.xml` 是二级功能面板，包含：

- 音质
- Hi-Fi
- Scratch
- 分享
- 下载
- 收藏
- 歌词
- 更多

`audio_player.xml` 的 `vp_song_handler` 和 `rb_bluetooth_dot` 命名说明底部至少有两个页面：

- 控制页
- 音频输出或路由页

`router_zone_layout.xml` 进一步确认了第二页的用途是音频输出切换。

### 4.4 队列与小播放条

队列结构由以下文件组成：

- `now_playing_layout.xml`
- `fake_song_layout.xml`
- `playing_now_item.xml`
- `now_playing_header_layout.xml`

小播放条结构由 `playback_bar.xml` 定义，核心是：

- 左侧封面缩略图
- 中部歌名/歌手
- 右侧收藏、上一首、播放/暂停、下一首

这部分很适合做成单独的 `MiniPlayerBar` 组件，后面接 Media3 状态即可。

## 5. 关键尺寸参考

建议优先以 `values-xxhdpi-v4/dimens.xml` 作为首个 1:1 复刻基准，因为播放页主位图也主要完整存在于 `drawable-xxhdpi-v4`。

播放页关键尺寸：

- 唱盘宽：`360dp`
- 唱盘高：`356.5938dp`
- 唱针宽：`73.3dp`
- 唱针高：`310dp`
- 顶部进度条高度：`41dp`
- 进度条左右内边距：`51.3dp`
- 唱针顶部边距：`25.5dp`
- 唱针右边距：`2.5dp`
- 唱针阴影右边距：`2dp`
- 更多按钮边距：`12dp`
- 灯光按钮顶部边距：`38dp`

适配资源也已经存在：

- `values-sw411dp-v13`
- `values-sw432dp-v13`
- `values-sw500dp-v13`

这意味着原版本身就维护了多窗口宽度策略，后续可以继续沿用。

## 6. 当前 Compose 工程现状（2026-04-28 核对）

当前 `app` 已经进入“可交互原型 + 播放页核心 1:1 资源复刻 + 列表页内容化推进”阶段。

### 6.1 已完成部分

- `MainActivity` 已接入音频权限申请，并以 `MusicApp` 作为应用壳层入口。
- `MusicApp` 已实现顶部标题栏、五标签底栏、全局 `GlobalPlaybackBar` 和全屏 `PlaybackScreen` 浮层。
- 壳层五标签底栏已完成底部安全区拆层、图标重心回正和浅灰底栏容器色校正，当前内容区与底栏边界在手势导航与三键导航场景下都更容易区分。
- `MusicNavHost` 已接入 `Playlist`、`Artist`、`Album`、`Songs`、`More` 五个一级页面。
- `PlaybackService`、`PlaybackControllerProvider`、`LocalAudioLibrary` 已接入 Media3 / ExoPlayer，本地音频已可浏览、可播放、可切歌。
- 壳层级全局搜索页已接入本地搜索历史、歌曲 / 专辑 / 艺术家分类结果与底部播放条联动；搜索框清除按钮已按原版 `clear_text` 资源比例放大到输入框高度内，并接入 `clear_text_down` 按下态。
- `SongsScreen` 已完成权限态、空态、本地歌曲列表、点击播放，以及壳层顶栏驱动的编辑态批量选择 / 从资料库隐藏。
- `AlbumScreen` 已完成权限态、空态、本地专辑聚合、116dp 平铺视图、61dp 列表视图、顶部列表 / 平铺切换、点击进入专辑详情、全部播放、随机播放与单曲播放。
- 专辑详情页已按原版截图完成 139dp 头部、116dp 封面、标题 / 艺术家 / 年份信息、添加到播放列表 / 添加到播放队列图标、全部播放 / 随机播放分栏和曲目列表；头部操作已经接入真实播放列表选择与队列追加。
- `ArtistScreen` 已完成权限态、空态、本地艺术家聚合、61dp 纯文本列表行、中文 Collator 排序、当前播放高亮、点击进入歌手详情、全部播放、随机播放与单曲播放。
- 艺术家详情页已复用现有二级页进入 / 返回动画和顶部返回栏，当前头部沿用专辑详情结构显示代表封面、歌手名、专辑 / 歌曲数量与添加到播放列表 / 添加到播放队列操作按钮，并已接入真实操作。
- `PlaylistScreen` 已按原版 `f_play_list.xml`、`f_playlist_listview.xml`、`lv_tracks.xml` 与 `local_song_action_playlist.xml` 落地用户播放列表模块，当前覆盖列表页、新建 / 重命名 / 删除播放列表、详情页、编辑态移除歌曲、添加歌曲页、单曲更多操作、贴底操作弹窗、加歌页纵向过渡和顶部返回链路。
- `PlaylistRepository` 已基于 Room 接入本地播放列表持久化，支持播放列表摘要、曲目顺序、重名校验、批量加歌、删除播放列表、移除列表内歌曲，以及物理删除 / 资料库隐藏后的跨歌单脏 `mediaId` 清理和顺序压紧。
- `MoreScreen` 已完成首屏三行入口复刻，并在壳层顶部栏接入原版设置图标；风格页、我喜欢的歌曲页、设置页与文件夹页都已接入现有 `SecondaryPageTransition` 二级页进入 / 返回动画。
- 文件夹页已接入 `FolderScreen` 二级页，基于 `MediaStore.RELATIVE_PATH` 聚合目录，支持目录详情、编辑态单项隐藏显示切换，以及从文件夹页工具栏触发的媒体库手动重扫；目录隐藏成功后会把该目录下歌曲统一回收到队列、收藏、用户歌单和全局列表刷新链路。
- `GenreTagRepository` 已完成本地风格聚合，`GenreScreen` 当前覆盖风格概览页、详情页、全部播放、随机播放与单曲播放。
- `PlaybackSettingsStore` 已接入 DataStore，当前统一持久化“打碟”“隐藏唱机中轴”“唱盘爆豆音”三项设置，更多页设置页和播放页更多面板会共用这一份状态。
- `LibraryExclusionsStore` 已接入 DataStore 持久化隐藏媒体 ID 与目录键，歌曲 / 专辑 / 艺术家 / 文件夹 / 搜索 / 喜欢 / 播放列表页会跟随排除集或壳层刷新版本重新取数，并与 `PlaybackService` 的 Media3 浏览结果同步过滤。
- `LocalAudioLibrary` 当前已按 `MediaStore version + generation` 维护媒体库快照缓存，`PlaybackService` 通过 Media3 自定义命令提供用户触发的手动重扫和轻量资料库失效通知，并由壳层把刷新版本分发给歌曲 / 专辑 / 艺术家 / 文件夹 / 搜索 / 喜欢 / 播放列表页面重新取数。
- `FavoriteSongsDatabase` / `FavoriteSongsRepository` 已基于 Room 持久化收藏歌曲，`LovedSongsScreen` 当前支持按收藏时间 / 歌名排序、全部播放、随机播放、单曲播放、取消喜欢、加入播放列表与加入播放队列。
- 专辑页同级列表 / 平铺切换采用 `Crossfade` 原地淡化，二级页进入 / 返回统一收敛到 `SecondaryPageTransition`，当前系统返回使用普通 `BackHandler`，避免预测性返回带来的闪烁。
- `PlaybackScreen` 已完成顶部进度条、唱盘与唱针主视觉、歌词遮罩切换、底部控制页与路由页 Pager、音量条、更多动作弹层、Scratch 拖拽预览，并补齐循环 / 单曲循环 / 随机播放的原版按钮状态与提示文案。
- 播放页返回键已收敛到 `PlaybackScreen`，更多面板优先关闭，再触发播放页收起。
- 播放页歌词遮罩已支持点击唱盘切换，并在歌词页隐藏唱针相关图层。
- 播放页已接入内嵌歌词解析与歌词页渲染，当前支持 `USLT`、`SYLT`、LRC 时间戳文本与常见自定义歌词键，歌词页会按播放进度自动居中当前行，并保留主歌 / 副歌之间的自然段空白。
- 播放页歌词页右上角常亮按钮当前已接入 Compose 官方 `Modifier.keepScreenOn()` 真实常亮能力，并补齐原版 `sun_btn_on` / `sun_btn_on_down` 选中态资源。
- Scratch 搓碟链路已接入当前曲目 PCM 擦盘输出，`ScratchSoundController` 使用 `MediaExtractor` / `MediaCodec` 解码，并用 `AudioTrack` 按拖动方向和速度播放。
- 播放页当前会按设置实时联动：打碟开关可同时控制设置页与更多面板入口，唱机中轴可按设置显隐，爆豆音会在播放时按随机间隔触发原版 `y_*_01.ogg` 短音效。
- 更多动作面板已按原版资源完成首轮两排网格复刻，包含标题栏、取消按钮、添加到播放列表、添加到播放队列、喜欢、歌词、设置铃声、定时关闭播放、打碟、删除等入口。
- 播放页“设置铃声”当前已按 Android 官方特殊权限流程接入 `WRITE_SETTINGS` 授权检查、系统设置跳转与 `RingtoneManager.setActualDefaultRingtoneUri()` 设置系统铃声；“定时关闭播放”已通过 `PlaybackService` 自定义 SessionCommand 与 `CountDownTimer` 接入 15 分钟、30 分钟、1 小时、1.5 小时、2 小时和关闭定时选项。
- 播放页“删除”当前已按 Android 官方共享媒体删除流程接入 `MediaStore.createDeleteRequest()`，点击后直接交给系统删除确认；CUE 整轨与无法解析为 MediaStore URI 的歌曲会拦截提示，删除成功后同步移除当前播放队列、收藏记录、用户歌单持久化脏数据，并触发 Media3 资料库失效和壳层全局列表刷新。
- 播放页、播放列表新建 / 重命名 / 选择弹窗，以及播放列表删除确认框当前已统一接入共享 `SmartisanDialog` 样式，分隔线改为带左右留白的 inset divider，动作区高度和标题 / 正文留白也已经收敛到统一节奏。
- 播放页更多动作、专辑详情、艺术家详情和播放列表详情页的“添加到播放列表 / 添加到播放队列”入口，当前都汇总到壳层统一处理，播放列表选择与新建播放列表弹窗会复用同一套状态。
- 播放页更多面板与 `GlobalPlaybackBar` 的收藏按钮当前统一接入 `FavoriteSongsRepository`，会和“我喜欢的歌曲”列表实时联动。
- 播放队列页当前已作为 `PlaybackScreen` 内部纵向切入浮层落地，包含标题栏、正在播放卡片、继续播放列表、清空继续播放和基于 Media3 `moveMediaItem` 的拖拽排序；左上角退出全屏播放，右上角回到全屏播放页。
- 唱针层已改用原版 `playing_stylus_lp*.9.png` 资源和 Android 原生 `FrameLayout + ImageView` 叠层，播放时落到唱片外圈，暂停时回到右侧停靠位。
- 唱片旋转已从播放器位置快照驱动改为 Compose 帧时钟驱动，并记录播放器位置采样时间进行实时校准，播放中视觉流畅度已明显提升；暂停、歌词页和 Scratch 预览状态仍按当前位置对齐。
- `GlobalPlaybackBar` 已复刻小播放条主体结构，并接入上一首、播放/暂停、下一首与收藏切换；收藏状态会跟随 `FavoriteSongsRepository` 持久化刷新。
- Media3 通知点击入口已通过 `MediaSession.Builder.setSessionActivity()` 指向播放页启动 Intent，`MainActivity` 支持 `onCreate` 与 `onNewIntent` 消费该入口，并由 `MusicApp` 展开全屏 `PlaybackScreen` 浮层；进程恢复与配置重建场景已通过 consumed 标记避免重复处理。

### 6.2 已导入的参考资源

- `app/src/main/res/drawable-nodpi/` 当前已有 `129` 个资源文件，已经覆盖唱盘、唱针、播放控制、小播放条、顶部按钮、空态、TabBar、专辑默认封面、专辑列表遮罩、专辑详情操作按钮、随机播放按钮、更多动作面板、搜索页按钮 / 标签 / 分段条资源和文件夹空态等核心资产。
- `app/src/main/res/drawable-xxhdpi/` 当前已有 `70` 个资源文件，包括音量控件资源、原版唱针 9-patch 资源、更多页箭头与 TabBar 图标、设置图标、风格页空态与随机播放按钮、播放列表按钮、我喜欢的歌曲页按钮与分段背景，以及文件夹隐藏显示眼睛动效序列帧资源。
- `app/src/main/res/raw/` 当前已有 `20` 个资源文件，已导入原版 `y_1_01` 到 `y_20_01` 黑胶爆豆音短样本，运行时 Scratch 声音仍来自正在播放曲目的 PCM 解码输出。
- 当前仓库已经直接使用的核心参考资源包括 `playing_lp`、`playing_cover_lp`、`playing_stylus_lp_original`、`playing_stylus_lp_bg_original`、`playing_stylus_lp_top_original`、`needle_shadow2`、`playing_control_time`、`playing_control_volume`、`btn_playing_prev/play/pause/next`、`btn_playing_shuffle_off/on`、`btn_playing_cycle_off/on`、`btn_playing_repeat_on`、`more_btn`、`sun_btn_off`、`floatplay_bg`、`floatplay_btn_*`、`now_playing_bar_shadow`、`playing_dot_*`、`mask_playing_lyric`、`noalbumcover_120/220`、`mask_albumcover_list`、`btn_display_list2`、`ablum_btn_addlist`、`ablum_btn_addplay`、`btn_shuffle3`、`more_select_*`、`arrow3`、`arrow3_down`、`tabbar_style*`、`tabbar_like*`、`tabbar_folder*`、`tabbar_setting*`、`search`、`btn_cancel_cm*`、`clear_text*`、`search_badge_grey*`、`search_clear`、`top_spinner_button_*`、`blank_folder`、`eye_icon_0001-0016`、`eye_open_anim`、`eye_close_anim`、`y_*_01.ogg`。

### 6.3 当前与原版的主要差距

- `More` 已完成首屏三行入口复刻、风格页、我喜欢的歌曲页、设置二级页和文件夹真实目录聚合；文件夹页当前已收敛为“编辑态单项显隐 + 手动重扫”交互，隐藏后的全局数据回收已经接入，更多批量管理能力仍待接入。
- `Playlist` 已接入本地用户播放列表、列表详情、加歌链路和统一全局搜索入口，评分 / 播放次数真实排序、收藏型播放列表和更完整的动作音效仍待推进。
- `Album` 已接入本地数据聚合、列表 / 平铺视图和详情二级页，编辑态、批量隐藏、补全封面、长按选择等原版管理能力待推进。
- `Artist` 已接入本地数据聚合、列表和详情二级页，原版在线歌手资料、地区 / 风格 / 简介区、歌手头像资源、右侧 QuickBar 索引、编辑态和批量管理能力待推进。
- 更多动作面板已完成原版资源风格复刻，当前仍待补齐音质、Hi-Fi、下载、更多等入口的真实业务能力。
- 歌词页已恢复原版遮罩、颜色和段落留白风格，歌词搜索、无歌词提示、更多原版过渡和不支持自动滚动提示仍待补齐。
- 路由页当前是界面态切换，后续需要绑定系统真实输出设备。
- 队列页主结构已经按 Compose 浮层接入并完成真实队列映射，后续仍可继续向原版列表分隔、拖拽反馈细节和头部卡片视觉做二轮校正。
- `collect.ogg`、`cancel.ogg` 以及铃声 / 下载 / 更多等动作音效仍待导入运行时资源并挂到真实交互链路。
- 原版自定义控件级行为，例如唱片启停 / 速度切换曲线、更多弹层过渡、部分触感反馈，还需要二轮代码逆向校正。

### 6.4 阶段判断

- “静态主播放页复刻”已经完成。
- “Media3 本地播放接入”已经完成。
- “播放页更多动作面板首轮复刻”已经完成。
- “Scratch 搓碟音频链路”已经完成。
- “原版唱针资源和播放 / 暂停状态落针逻辑”已经完成。
- “播放页唱片帧同步旋转优化”已经完成。
- “系统媒体通知点击回到全屏播放页”已经完成。
- “专辑页本地数据聚合与列表 / 平铺视图”已经完成。
- “专辑详情二级页与普通返回动画”已经完成。
- “艺术家页本地数据聚合与歌手详情二级页”已经完成。
- “更多页首屏入口复刻”已经完成。
- “风格页本地流派聚合、详情页与随机播放”已经完成。
- “我喜欢的歌曲页与收藏持久化链路”已经完成。
- “更多页设置二级页与播放设置持久化联动，以及有封面歌曲的内圈专辑图 / 隐藏中轴联动修正、尺寸校正、中轴中心裁切修正、播放页进度条滑块对齐修正、底部音量滑轨原版资源还原和唱盘旋转平滑修正”已经完成。
- “文件夹页目录聚合与资料库隐藏管理首轮接入”已经完成。
- “媒体库手动重扫链路与文件夹页工具入口”已经完成。
- “删除本地歌曲与隐藏文件夹后的队列 / 收藏 / 用户歌单 / 全局列表回收链路”已经完成。
- “歌曲页编辑态批量隐藏管理”已经完成。
- “播放列表模块落地、本地持久化与二轮 UI 细节校正”已经完成。
- “播放页内嵌歌词解析与歌词页段落留白、当前行居中修正”已经完成。
- “壳层标题栏按钮与更多页二级页返回按钮统一校正”已经完成。
- “壳层标题栏按钮质感重做”已经完成。
- “壳层级全局搜索页、本地搜索历史与歌曲 / 专辑 / 艺术家结果链路”已经完成。
- “搜索页输入框清除按钮尺寸与按下态校正”已经完成。
- “播放队列页、继续播放清空与拖拽排序链路”已经完成。
- “播放页删除本地歌曲的系统 MediaStore 删除确认链路”已经完成。
- 当前主线最适合继续推进“真实路由 + More 剩余内容化 + 专辑 / 艺术家管理能力 + 原版音效资源 + 播放列表二轮细节校正”。

### 6.5 最近提交同步

- `fix: 收敛删除与隐藏后的资料库回收`
  - 播放页系统删除成功后统一清理当前队列、收藏记录和所有用户歌单中的脏 `mediaId`，并通过 Media3 资料库失效通知推动全局页面重拉。
  - 文件夹页隐藏目录成功后会计算目录内歌曲集合，复用同一套队列 / 收藏 / 歌单 / 资料库刷新回收链路，避免搜索、喜欢页和歌曲 / 专辑 / 艺术家列表残留。
  - 补充歌单顺序压紧和文件夹目录匹配测试，并通过 `testDebugUnitTest`、`assembleDebug` 与 `git diff --check` 验证。
- `fix: 完善播放页循环与随机控制`
  - 将播放页循环按钮补齐为“关闭 -> 循环播放 -> 单曲循环 -> 关闭”的原版三态顺序，并接入 `btn_playing_repeat_on` 单曲循环图标。
  - 将播放页循环 / 随机按钮的开关反馈补齐为原版 Toast 文案，覆盖“循环播放已开启 / 单曲循环已开启 / 循环播放已关闭 / 随机播放已开启 / 随机播放已关闭”。
  - 将循环与随机按钮资源恢复为原版 9-patch 形态，避免 release 资源优化后普通 PNG 拉伸导致图标尺寸异常。
- `chore: 准备 1.0.1 发布`
  - 将复刻应用版本从 `1.0.0` 提升到 `1.0.1`，用于承接 `v1.0.0` 之后的稳定性修复与文档同步。
  - 将 Android 内部 `versionCode` 从 `100` 提升到 `101`，确保新 APK 可覆盖安装旧版本。
  - 发布标签计划同步为 `v1.0.1`，方便 GitHub Release 关联本次补丁发布。
- `fix: 接入播放页系统删除确认`
  - 将播放页更多面板的“删除”入口从资料库隐藏改为 Android 官方 `MediaStore.createDeleteRequest()`，点击后直接进入系统删除确认。
  - 保留 CUE 整轨和无法解析媒体 URI 的限制提示，避免对不适合单曲删除的资源发起系统删除请求。
  - 系统确认删除成功后同步从当前 Media3 队列移除同一 `mediaId`，并清理对应收藏记录。
- `80d9930 fix: 放大搜索页清除按钮`
  - 将搜索输入框清除按钮的可点击区域调整为与搜索框高度一致，避免原先 `24dp` 容器加内边距导致视觉尺寸过小。
  - 使用原版 `clear_text` 与 `clear_text_down` 资源切换普通和按下状态，让清除操作反馈更贴近原版搜索框。
  - 补充“清空搜索内容”无障碍文案，避免继续复用“清空历史”描述。
- `fix: 优化播放页唱片旋转流畅度`
  - 将播放页唱片旋转收敛为视觉时钟驱动，播放、暂停和切歌都沿用当前视觉角度，减少播放器位置快照带来的跳动。
  - 去掉 360 度归一化边界，旋转角度持续递增，并把帧时间计算从整数毫秒改为浮点毫秒，提升匀速感。
  - 保留 Scratch 搓碟预览时按位置同步角度，搓碟音频周期与测试断言同步到当前唱片周期。
- `fix: 接入媒体库手动重扫并收敛文件夹页显隐交互`
  - 为 `LocalAudioLibrary`、`PlaybackService` 和 Media3 Controller 增加用户触发的媒体库重扫命令，手动刷新后歌曲、专辑和文件夹页会重新取数。
  - 将重扫入口收口到“更多 > 文件夹”工具栏，移除歌曲页和专辑页顶栏的临时按钮，同时让文件夹页跟随刷新版本同步目录聚合结果。
  - 清理文件夹页编辑态的批量删除与勾选逻辑，改为仅保留单项眼睛显隐，并按原版逆向资源比例将眼睛图标调整到约 `42x34dp`。
- `fix: 补齐歌曲页专辑信息与应用名`
  - 在歌曲页列表项补上 `歌手 - 专辑` 副标题，并复用原版无损格式徽标资源展示 `FLAC / APE / WAV / AIFF / ALAC / CUE`。
  - 扩展本地音频扫描元数据，增加 `DISPLAY_NAME` 与 `MIME_TYPE` 识别逻辑，将无损格式结果写入 `MediaMetadata.extras` 供列表 UI 直接消费。
  - 将应用名资源从 `Music` 调整为 `音乐`，让桌面图标与系统任务切换标题和原版命名保持一致。
- `feat: 新增播放队列页`
  - 在 `PlaybackScreen` 内接入纵向切入的播放队列浮层，补齐顶部标题栏、正在播放卡片、继续播放列表和返回交互。
  - 将继续播放列表映射到 Media3 当前队列，支持点击切歌、清空后续队列和通过右侧手柄拖拽排序。
  - 调整队列页顶部按钮语义和尺寸，移除当前播放卡片的评分占位点，统一和全屏播放页的工具栏质感。
- `fix: 校正底栏安全区与视觉分层`
  - 拆分底栏可视表面与 `navigationBars` 安全区占位，恢复底栏图标与文字组合在 54dp 可视区内的垂直重心。
  - 去掉上沿阴影方案，改用更浅的灰色底栏容器色和同色系统栏占位，让内容区与底栏边界更稳定可辨。
  - 保持五标签点击热区、横向安全区与外部调用方式不变，影响范围集中在壳层底栏组件。
- `234e9a0 fix: 优化标题栏按钮质感`
  - 在 `SmartisanPageScaffold` 内重做顶部按钮的渐变、描边、阴影和按压态，让返回、文字和图标按钮共享更统一的轻拟物层级。
  - 微调标题字号、按钮高度、圆角、内边距和返回箭头笔画，收紧一级页与二级页顶部栏的比例关系。
  - 保持页面调用逻辑不变，影响范围集中在壳层公共标题栏组件，便于继续做细节迭代。
- `fix: 接入壳层全局搜索页与本地搜索链路`
  - 在壳层新增全局搜索浮层，统一接管壳层标题栏、更多页二级页和播放列表页标题栏的搜索入口，进入 / 退出动画与全屏播放页保持同轴纵向切场。
  - 新增本地搜索数据与历史持久化，支持空查询历史态、歌曲 / 专辑 / 艺术家分类结果、搜索结果播放 / 跳转，以及搜索页内底部全局播放条联动。
  - 导入并复用原版搜索页位图资源，补齐输入法 `adjustResize` 与搜索浮层退出动画状态重置，修正九宫格资源加载与退出闪烁问题。
- `82e1d4a fix: 统一标题栏按钮视觉`
  - 将壳层与播放列表页标题栏的编辑、搜索、设置、删除、视图切换按钮统一收敛到同一套工具栏按钮容器，补齐更多页、文件夹页、设置页、风格页和歌曲编辑态的按钮样式一致性。
  - 将更多页二级页左侧返回按钮改成“自绘返回箭头 + 文案 + 工具栏描边外框”的组合，去掉对逆向返回位图的依赖，统一颜色、尺寸和交互按压态。
  - 对标题栏按钮外框、图标尺寸和浅色描边做二轮微调，让一级页与二级页在真机上更接近原版层级和轻重关系。
- `docs: 补充仓库说明与真机截图`
  - 在仓库根目录新增 `README.md`，公开项目目标、主 Prompt、技术架构、当前依赖版本和目录结构，方便后续协作与对外说明。
  - 新增 `docs/images/device-playback-real-shot.jpg` 真机播放页截图，让当前复刻效果具备稳定的可视化展示入口。
- `ca7a691 fix: 接通播放页歌词常亮按钮`
  - 按 Android 官方屏幕常亮文档与 Compose `Modifier.keepScreenOn()` API，将歌词页右上角常亮按钮接到真实常亮逻辑，作用域收敛到歌词页可见组合树。
  - 补齐原版 `sun_btn_on` / `sun_btn_on_down` 资源，并让歌词页按钮按开关状态切换原版 on / off 视觉。
  - 通过 Subagent 二轮 review 与 `:app:compileDebugKotlin`、`:app:testDebugUnitTest`、`:app:assembleDebug` 本地验证。
- `a9633c0 feat: 接入我喜欢的歌曲与收藏持久化`
  - 新增 `FavoriteSongsDatabase`、`FavoriteSongsRepository` 与收藏数据测试，收藏状态当前由 Room 持久化并提供实时 Flow。
  - 在更多页接入“我喜欢的歌曲”二级页，支持按收藏时间 / 歌名排序、全部播放、随机播放、单曲播放、取消喜欢、加入播放列表和加入播放队列。
  - 统一播放页更多面板、小播放条和“我喜欢的歌曲”页面的收藏状态来源，补齐对应文案与位图资源。
- `5fcf4f8 feat: 完善播放页铃声定时与弹窗样式`
  - 为播放页更多面板接入“设置铃声”和“定时关闭播放”真实业务，按 Android 官方文档补齐 `WRITE_SETTINGS` 特殊权限流程、`RingtoneManager.setActualDefaultRingtoneUri()` 铃声设置，以及基于 `CountDownTimer` 和 Media3 自定义命令的后台睡眠定时。
  - 新增 `SmartisanDialogs` 共享弹窗壳层，统一播放页铃声确认框、睡眠定时弹窗、播放列表新建 / 重命名 / 选择弹窗和删除确认框的标题、动作区与 inset divider 风格。
  - 补充睡眠定时格式化单测，并通过 `:app:testDebugUnitTest` 与 `:app:assembleDebug` 本地验证。
- `063997f feat: 完善风格页复刻`
  - 新增 `GenreTagRepository`、`GenreModels` 与对应测试，基于媒体元数据完成本地风格聚合与排序。
  - 在更多页接入 `GenreScreen`，覆盖风格概览页、详情页、全部播放、随机播放和单曲播放链路。
  - 补齐风格页空态和顶部随机播放按钮资源，让更多页内容化继续向原版推进。
- `f8e05ae fix: 调整底部导航切页过渡`
  - 为底部一级页切换补齐导航方向判断，让切页动画与当前 tab 位置保持一致。
  - 壳层一级页切换体验已经和现有二级页过渡拆分，页面切换语义更稳定。
- `c9e6663 feat: 完成歌曲管理与播放列表模块迁移`
  - `SongsScreen` 已接入编辑态批量选择与从资料库隐藏，壳层顶部栏会根据一级页状态切换编辑 / 完成 / 删除动作。
  - `PlaylistScreen` 已接入播放列表列表页、详情页、加歌页、新建 / 重命名 / 删除 / 移除歌曲、单曲更多操作和队列追加链路，并补齐原版按钮资源、加歌页纵向动画、空列表“添加歌曲”入口和新建后直达加歌页。
  - `PlaylistRepository`、`PlaylistDatabase`、相关字符串、弹窗、导航参数和 Room 持久化接线已经完成，并通过 `compileDebugKotlin`、`testDebugUnitTest` 与 `assembleDebug` 本地验证。
- `b99da27 fix: 修复播放页歌词解析与歌词页排版`
  - 新增 `EmbeddedLyrics` 解析链路，覆盖 `USLT`、`SYLT`、LRC 时间戳文本与常见自定义歌词键。
  - 歌词页当前支持按播放进度自动居中当前行，并恢复主歌 / 副歌之间的自然段留白。
  - 新增歌词解析与渲染模型测试，覆盖时间轴与展示排版核心逻辑。
- `ef92351 feat: 完善设置页与播放页联动`
  - 新增 `PlaybackSettingsStore` 与 `SettingsScreen`，统一持久化打碟、隐藏唱机中轴和唱盘爆豆音设置。
  - 播放页更多面板、设置页和主播放 UI 已共享同一份设置状态，音量滑轨和相关位图资源也已对齐原版。
  - 导入 `y_1_01` 到 `y_20_01` 黑胶爆豆音短样本，并接入播放中的随机触发逻辑。
- `11ec7d2 feat: 接入文件夹页与资料库隐藏管理`
  - 在更多页接入文件夹二级页、目录详情和编辑态批量隐藏入口，补齐空态资源、眼睛动效与顶部危险操作按钮。
  - 引入 `LibraryExclusionsStore` 持久化隐藏媒体 ID 与目录键，让 `PlaybackService`、歌曲、专辑、艺术家和文件夹列表统一跟随排除集刷新。
  - 将播放页删除操作接到“从资料库隐藏”，补充目录过滤单元测试与 DataStore 依赖，并恢复 `gradlew` 可执行权限便于本地构建。
- `8a12898 feat: 完善更多页面复刻`
  - 实现更多页首屏三行入口布局，补齐风格、我喜欢的歌曲、文件夹条目及按压态图标切换。
  - 在壳层顶栏为更多页接入“设置”文字按钮，并新增对应文案与箭头、TabBar 位图资源。
  - 将更多页状态从占位展示推进到首轮 1:1 结构复刻，为后续接入真实业务入口和下钻页面提供基础。
- `ca5188d feat: 实现艺术家页面复刻`
  - 实现本地艺术家聚合、专辑 / 歌曲数量统计、中文排序和 61dp 艺术家列表行。
  - 接入歌手详情二级页，补齐代表封面、歌手信息、全部播放、随机播放、曲目列表和普通返回动画。
  - 将艺术家详情顶部栏接入现有 `SecondaryPageTransition`，保持和专辑详情一致的进入 / 返回体验。
  - 新增艺术家聚合单元测试，覆盖艺术家分组、专辑数量统计、未知艺术家兜底和中文排序。
- `bc3a2b7 feat: 完善专辑页面复刻`
  - 实现本地专辑聚合、列表 / 平铺视图、封面读取、当前播放高亮和 `Crossfade` 原地切换动画。
  - 接入专辑详情二级页，补齐头部信息、原版操作按钮、全部播放、随机播放、曲目列表和普通返回动画。
  - 抽取 `AudioPermission`、`EmbeddedArtwork`、`SecondaryPageTransition` 等共享组件，更新 `GlobalPlaybackBar` 与 `SongsScreen` 的权限 / 封面复用逻辑。
  - 新增专辑聚合单元测试，覆盖专辑分组、曲目排序、年份读取和唱片音轨编号展示逻辑。
  - 更新 `AGENTS.md`，明确提交前等待用户测试确认。
- `d4e0fb0 fix: 修复通知点击返回播放页`
  - 为 Media3 播放会话配置打开播放页的 session activity，确保系统媒体通知点击进入全屏播放界面。
  - 在 `MainActivity` 中统一消费播放页启动 Intent，并通过 consumed 标记覆盖进程恢复与配置重建场景。
  - 将播放页启动请求传入 `MusicApp`，驱动现有 `PlaybackScreen` 浮层展开，影响范围限定在通知入口与播放页壳层状态。
- `805f692 fix: 提升播放页黑胶唱片旋转流畅度`
  - 将唱片旋转从播放器位置快照改为 Compose 帧时钟驱动，减少角度跳变带来的卡顿感。
  - 在播放状态记录位置采样时间，用实时增量校准旋转角度。
  - 保留暂停、歌词页、Scratch 拖拽预览状态下的当前位置对齐逻辑。
- `a7c3579 feat: 完善播放页更多操作与唱针还原`
  - 按原版资源补齐播放页更多操作面板的标题栏、操作网格、按钮图标与文案。
  - 使用原版唱针 9-patch 资源和 Android 原生叠层还原唱针底座、本体、阴影与顶盖。
  - 调整唱针播放、暂停和划碟状态的落针逻辑，暂停时回到停靠位置。
- `97ffff1 chore: 清理 IDE 配置跟踪`
  - 将 `.idea/` 加入忽略规则，从索引移除已跟踪 IDE 配置，并新增 `AGENTS.md` 项目协作说明。
- `012c85d fix: 修复播放页歌词与按钮对齐`
  - 修正更多按钮、常亮按钮、歌词遮罩和唱盘点击切换。
- `c0b1859 fix: 修复播放页返回键行为`
  - 将播放页返回键处理收敛到 `PlaybackScreen`。
- `fb77ed8 fix: 还原搓碟音频链路`
  - 接入当前曲目 PCM 擦盘输出、精准 seek 模式、搓碟速率映射和单测。

## 7. 后续逆向建议

如果后面需要 1:1 还原这些行为，建议补齐同版本 ROM 的系统文件：

- 唱针摆角动画
- 唱片启停与速度切换曲线
- Scratch 交互逻辑
- 黑胶杂音触发条件
- 歌词切换与遮罩过渡
- 页面翻页和弹层过渡

需要的材料优先级：

1. 同版本 Smartisan OS 系统镜像
2. 对应目录下的 `odex` / `vdex` / `oat`
3. 预装音乐应用所在系统分区完整目录

拿到这些文件后，可以继续做第二轮逆向：

1. 提取 DEX
2. 补全 Activity / View / Service 代码
3. 追踪播放页自定义控件行为
4. 校正 Compose 动画曲线与触摸反馈

## 8. 最直接的开发顺序

建议按这个顺序推进：

1. 按 `now_playing_layout.xml`、`fake_song_layout.xml`、`playing_now_item.xml` 落地队列页
2. 按 `router_zone_layout.xml` 接入真实输出设备路由
3. 让 `Playlist`、`More` 从空态进入真实数据与 1:1 布局复刻，并继续补齐 `Album` / `Artist` 编辑态、批量管理和封面 / 歌手头像补全
4. 补齐 `collect.ogg`、`cancel.ogg` 以及剩余参考资源
5. 为更多动作面板接入音质、Hi-Fi、下载、更多等剩余真实业务能力
6. 在拿到 `odex` / `vdex` / `oat` 后校正唱片启停曲线、速度切换曲线、歌词过渡、更多弹层过渡与触摸反馈

当前这一步最值得马上开始的是：把 `now_playing_layout.xml` 对应的队列页补齐到原版结构，再接入真实路由。
